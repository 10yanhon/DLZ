# Neutral Shop — Pages + Functions (Deployable)

This repo is a fully working demo e-commerce front-end (static) + back-end (Cloudflare Pages Functions).
After adding these files to GitHub, connect this repository to Cloudflare Pages (via "Create a project").

Deployment notes:
- Framework preset: None
- Build command: (leave empty)
- Build output directory: `.`

Pages Functions: files under `functions/api/*.js` are deployed automatically and available at `/api/*`.
